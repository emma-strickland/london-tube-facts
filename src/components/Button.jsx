import React from 'react'

const Button = (label) => {
  // I want to create a button that can toggle between museums and clubs...
  // when the user selects museums, only the museum markers will show on the map
  return (
    <button type="submit" className="button">Click me!</button>
  )
}

export default Button;